const params=new URLSearchParams(location.search);const id=+(params.get('mt')||1);function show(){let raw=localStorage.getItem('mt-risk-public-v2');let d=raw?JSON.parse(raw):null;let mt=d?.mts?.find(x=>x.id===id);document.getElementById('team').textContent=`MT ${id}`;if(!mt){document.getElementById('title').textContent='Nog geen actuele risicokaart beschikbaar';document.getElementById('description').textContent='Open eerst de docentbediening en start de simulatie.';return}document.getElementById('round').textContent=`Ronde ${mt.round}`;document.getElementById('risk').textContent=`Actuele risicokaart · ${mt.risk}`;document.getElementById('title').textContent=mt.title;document.getElementById('description').textContent=mt.description;document.getElementById('meta').textContent=`Afzender: ${mt.sender} · Risico-eigenaar: ${mt.owner}`}show();window.addEventListener('storage',show);
